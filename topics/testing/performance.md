# Performance Testing

🧠 What it is:
- Measuring how fast your frontend loads, renders, and responds to user actions.

🎯 Purpose:
- Guarantee smooth UX and optimize performance metrics (LCP, FID, CLS).

⚙️ Tools:
- `Lighthouse` (Chrome DevTools)
- `WebPageTest`
- `Sitespeed.io`

💻 Example:
- Use Lighthouse CI to fail builds if performance score < 90.